# 在云端分别打 macOS / Windows 安装包（无需本机跨平台）
name: Desktop installers

on:
  workflow_dispatch:
  push:
    tags:
      - "desktop-*"

permissions:
  contents: read

jobs:
  macos-arm64:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: desktop/package-lock.json

      - name: Install and build (macOS arm64)
        env:
          CSC_IDENTITY_AUTO_DISCOVERY: "false"
        working-directory: desktop
        run: |
          npm ci
          npm run dist:mac:arm64

      - uses: actions/upload-artifact@v4
        with:
          name: animal-adoption-macos-arm64
          path: |
            desktop/release/*.dmg
            desktop/release/*-mac.zip
            desktop/release/*-mac.zip.blockmap
          if-no-files-found: error

  macos-x64:
    runs-on: macos-13
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: desktop/package-lock.json

      - name: Install and build (macOS x64)
        env:
          CSC_IDENTITY_AUTO_DISCOVERY: "false"
        working-directory: desktop
        run: |
          npm ci
          npm run dist:mac:x64

      - uses: actions/upload-artifact@v4
        with:
          name: animal-adoption-macos-x64
          path: |
            desktop/release/*.dmg
            desktop/release/*-mac.zip
            desktop/release/*-mac.zip.blockmap
          if-no-files-found: error

  windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: desktop/package-lock.json

      - name: Install and build (Windows installer + portable)
        working-directory: desktop
        run: |
          npm ci
          npm run dist:win
          npm run dist:win:portable

      - uses: actions/upload-artifact@v4
        with:
          name: animal-adoption-windows-installer
          path: desktop/release/*Setup*.exe
          if-no-files-found: error

      - uses: actions/upload-artifact@v4
        with:
          name: animal-adoption-windows-portable
          path: |
            desktop/release/*.exe
            !desktop/release/*Setup*.exe
          if-no-files-found: error
